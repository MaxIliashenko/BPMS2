@page "/tasks/{id}/{header}"

<TaskListComponent FolderId="@id" Header="@header" />

@code {
    [Parameter]
    public string id { get; set; }
    [Parameter]
    public string header { get; set; }
}